"""Models for the menus domain.

Menu items, courses and the catalogue events draw from.

No models yet — the schema is deliberately not designed at this stage.
"""

from django.db import models  # noqa: F401
