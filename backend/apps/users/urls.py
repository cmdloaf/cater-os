"""URL routes for the users domain.

Registered under /api/v1/users/ once this app exposes endpoints. The empty
urlpatterns keeps the module importable so config/urls.py can include it
without a conditional.
"""

urlpatterns = []
