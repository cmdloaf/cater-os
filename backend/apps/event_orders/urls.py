"""URL routes for the event_orders domain.

Registered under /api/v1/event_orders/ once this app exposes endpoints. The empty
urlpatterns keeps the module importable so config/urls.py can include it
without a conditional.
"""

urlpatterns = []
