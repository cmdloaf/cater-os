"""URL routes for the files domain.

Registered under /api/v1/files/ once this app exposes endpoints. The empty
urlpatterns keeps the module importable so config/urls.py can include it
without a conditional.
"""

urlpatterns = []
