{
  "name": "vero",
  "version": "0.1.0",
  "private": true,
  "description": "Vero — the operating system for catering businesses.",
  "workspaces": [
    "apps/*",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace vero-web",
    "build": "npm run build --workspace vero-web",
    "start": "npm run start --workspace vero-web",
    "lint": "npm run lint --workspace vero-web"
  },
  "engines": {
    "node": ">=20"
  }
}
